; === Upload ===
upload_max_filesize = 100M
post_max_size       = 200M

; === Memory & Execution ===
memory_limit       = 256M
max_execution_time = 300
max_input_time     = 300
max_input_vars     = 10000

; === Error (dev default, prod override di Dockerfile) ===
display_errors         = On
display_startup_errors = Off
log_errors             = On
error_reporting        = E_ALL & ~E_DEPRECATED & ~E_STRICT

; === Timezone ===
date.timezone = Asia/Jakarta

; === Session ===
session.gc_maxlifetime = 7200

; === OPcache ===
opcache.enable                = 1
opcache.memory_consumption    = 256
opcache.max_accelerated_files = 20000
opcache.revalidate_freq       = 2
opcache.validate_timestamps   = 1
; prod akan override validate_timestamps=0 via Dockerfile



; max_input_vars=10000
; upload_max_filesize=100M
; post_max_size=200M

; memory_limit=256M
; max_execution_time=300
; max_input_time=300


; display_errors=On
; zend.exception_ignore_args = On
; display_startup_errors = Off
; log_errors = On
; error_reporting = E_ALL & ~E_DEPRECATED & ~E_STRICT


; opcache.enable=1
; opcache.memory_consumption=256
; opcache.max_accelerated_files=20000
